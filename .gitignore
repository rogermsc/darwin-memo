__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
dist/
build/
memory.json
my-memory.json
*.json.lock
memory-model/
.DS_Store
bench/results/smoke.json
bench/results/scaling.json
.coverage
.hypothesis/
.mypy_cache/
.ruff_cache/
examples/*.jsonl
.darwin-memo/lessons.events.jsonl

# distill arm local artifacts
.venv-distill/
memory-model*/

# arxiv-paper branch: local venvs, secrets, paper build artifacts
.venv312/
.swebench-key
.swebench-repos/
# Fetched, not authored: the loader verifies it against the sha256 in
# bench/swebench_cl/manifests/pilot.json, so the pin is the evidence
# and the 5.8MB payload is a cache.
.swebench-data/
*.env
paper/*.pdf
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out

# Resume checkpoints and raw model transcripts for the sampled LLM
# suites: regenerable, and a 3-model grid is tens of megabytes. The
# committed evidence is the results JSON plus its manifest entry.
bench/results/runs/
bench/results/transcripts/
.venv-organic/

# Built by ui/; shipped in the wheel, never committed.
darwin_memo/data/ui/

# uv resolves per-invocation here; a lockfile in a published library
# pins nobody and only churns. Swept in by accident in #50.
uv.lock
