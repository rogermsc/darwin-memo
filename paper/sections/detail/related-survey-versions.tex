% Why the bare identifier for this survey does not resolve to the version
% the paper quotes. Bibliographic, not argumentative: it moves to the
% appendix in the venue build and stays in the preprint where a reader
% checking the quotation is standing.
Its second version is a rewrite roughly a fifth the length, which
replaces the availability-gap analysis with a governance framework, so
the sentence we quote is no longer in the version the bare identifier
resolves to. The three availability threats v1 names are write flooding,
retrieval-latency poisoning and reflection-loop denial---none of them
ours, which sharpens rather than softens the point: denial of memory by
corrupted settlement was not on the list of unstudied threats either.
