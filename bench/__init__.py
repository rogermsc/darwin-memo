"""Benchmark harness for darwin-memo. Stdlib only, not installed with the package.

Run suites with ``python -m bench.run`` and render results with
``python -m bench.report``. See docs/benchmarks.md for the standing
results and the exact commands that produced them.
"""
